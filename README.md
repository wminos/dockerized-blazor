# asp.net core blazor / dockerizing / dotnet watch sample

## run dockerized app with dotnet watching

```cmd
docker-compose -f docker-compose.yml -f docker-compose.override.yml up --build
```

## todo

* Supports for IDE (dotnet watching)
